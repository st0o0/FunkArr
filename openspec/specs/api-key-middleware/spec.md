## Purpose

~~Removed.~~ API key authentication middleware was removed. FunkArr does not need authentication — the API key exists only because Sonarr/Radarr require one as a mandatory field. FunkArr runs in a Docker network where all callers are trusted. All endpoints are publicly accessible.

## Requirements

*No active requirements — all requirements were removed by the setup-wizard-rework change.*
