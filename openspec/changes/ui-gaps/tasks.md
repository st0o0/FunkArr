- [x] Add `GET /api/system/routes` endpoint returning RoutingOptions as read-only JSON
- [x] Add `GET /api/downloads/settings` client function to `downloads.ts`
- [x] Add `GET /api/system/routes` client function to `setup.ts`
- [x] Create `RingBufferSink` (Serilog ILogEventSink, ConcurrentQueue, last 500 entries)
- [x] Register RingBufferSink in LoggingSetupContainer
- [x] Add `GET /api/system/logs` endpoint returning ring buffer contents
- [x] Add `GET /api/system/logs/stream` SSE endpoint pushing new log entries
- [x] Create `useLogStream` composable following useQueueStream pattern
- [x] Create `DownloadDetail.vue` view with live progress from SSE + history fallback
- [x] Make Activity queue cards and history rows clickable (navigate to `/activity/:id`)
- [x] Create `Settings.vue` view with Downloads, Cache, Routes, System, Logs sections
- [x] Add Settings route to router and sidebar navigation
- [x] Add i18n keys for settings and download detail in all 4 locale files
- [x] Build frontend and verify all backend tests pass
